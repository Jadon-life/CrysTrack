# CrysTrack

CrysTrack is a personal productivity application built with Next.js,
TypeScript, Supabase and PostgreSQL.

## Project location

The deployable Next.js application lives inside:

`crystrack/`

The repository root itself is not the Next.js project root.

## Local development

From the repository root:

```bash
cd crystrack
